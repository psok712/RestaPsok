server:
  port: 8080

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/psok"
    username: "psok"
    password: "712"
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true